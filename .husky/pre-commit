#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for code quality
npx lint-staged

# Type checking disabled temporarily due to strict TypeScript config
# npm run type-check